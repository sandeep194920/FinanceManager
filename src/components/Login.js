import React from "react";
import useStyles from "./styles/RegisterLoginStyles";

function Login(props) {
  const classes = useStyles();

  return <div className={classes.container}>Login component</div>;
}
export default Login;
